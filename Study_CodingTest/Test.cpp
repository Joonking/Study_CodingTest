#include <iostream>
#include <queue>

using namespace std;

int N, M;
int pan[1000][1000];

int main() 
{
	ios::sync_with_stdio(false);
	cin.tie(NULL);
	cout.tie(NULL);

	cin >> M >> N;

	for (int i = 0; i < N; i++)
	{
		for (int k = 0; k < M; k++)
		{
			cin >> pan[i][k];
		}
	}






}








